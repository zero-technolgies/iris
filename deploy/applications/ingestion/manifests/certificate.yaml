apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ingestion-tls
  namespace: postgres
spec:
  secretName: ingestion-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - ingestion.iris.calebache.com
